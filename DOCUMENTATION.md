# نظام إدارة فندق الأصيل — التوثيق الشامل

> **Alaseel PMS — Hotel Property Management System**
> وثيقة مرجعية كاملة: ما تم بناؤه فعلياً، كيف يعمل، ما هي الثغرات الحالية، وما تبقّى للتنفيذ.
>
> | | |
> |---|---|
> | **اسم الحزمة** | `alaseel-pms` |
> | **الإصدار الحالي** | `0.1.0-phase1` (رغم أن الكود وصل عملياً للمرحلة 4) |
> | **إصدار مخطط البيانات** | `schema_version: 11` |
> | **المنصّة الأصلية** | Electron 31 (سطح المكتب — Windows / NSIS) |
> | **الحالة الحالية للمستودع** | نسخة **Web Demo** بمحاكاة LocalStorage |
> | **اللغة** | عربي بالكامل — RTL |
> | **الفرع** | `claude/hotel-system-documentation-eyuslm` |
> | **حجم الكود** | ~7,356 سطر عبر 21 ملفاً |

---

## جدول المحتويات

1. [نظرة عامة على النظام](#1-نظرة-عامة-على-النظام)
2. [حالة المشروع الآن — نسختان متوازيتان](#2-حالة-المشروع-الآن--نسختان-متوازيتان)
3. [البنية التقنية والمعمارية](#3-البنية-التقنية-والمعمارية)
4. [نموذج البيانات الكامل](#4-نموذج-البيانات-الكامل)
5. [نظام الحماية والحسابات](#5-نظام-الحماية-والحسابات)
6. [الوحدات المنفّذة — تفصيل كامل](#6-الوحدات-المنفّذة--تفصيل-كامل)
7. [التدفقات المشتركة بين الوحدات](#7-التدفقات-المشتركة-بين-الوحدات)
8. [واجهة المستخدم ونظام التصميم](#8-واجهة-المستخدم-ونظام-التصميم)
9. [خريطة الملفات ومسؤولياتها](#9-خريطة-الملفات-ومسؤولياتها)
10. [الثغرات والمشاكل الحالية](#10-الثغرات-والمشاكل-الحالية)
11. [ما لم يُنفّذ بعد — خارطة الطريق](#11-ما-لم-يُنفّذ-بعد--خارطة-الطريق)
12. [التشغيل والبناء والنشر](#12-التشغيل-والبناء-والنشر)
13. [المصطلحات](#13-المصطلحات)

---

## 1. نظرة عامة على النظام

**الأصيل PMS** هو نظام إدارة فندقية متكامل (Property Management System) مصمَّم للعمل **أوفلاين بالكامل** على جهاز واحد داخل الفندق، بدون أي خدمة سحابية أو خادم خارجي أو قاعدة بيانات منفصلة. كل بيانات الفندق تعيش في **ملف JSON واحد محلي**.

### الفلسفة التصميمية الحاكمة

النظام مبني على مجموعة قواعد صارمة، وكل سطر كود يحترمها:

| القاعدة | التطبيق العملي |
|---|---|
| **نافذة واحدة فقط (Strict Single-Frame SPA)** | لا يوجد أي `popup` أو `modal` أو نافذة ثانية على الإطلاق. كل التنقل هو استبدال محتوى `#contentPane`. حتى تأكيدات الحذف تُرسم *داخل* الصفحة (`inline-confirm`). Electron نفسه يرفض فتح أي نافذة عبر `setWindowOpenHandler(() => ({ action: 'deny' }))` — `src/main.js:314` |
| **أوفلاين 100%** | لا API خارجي، لا قاعدة بيانات، لا إنترنت مطلوب (باستثناء تحميل الخطوط من Google Fonts في `index.html:8`) |
| **العربية أولاً** | `<html lang="ar" dir="rtl">` — كل النصوص مركزية في `src/js/i18n.js` (821 سطراً) |
| **صدق البيانات المحاسبية** | النظام **يرفض** اختراع أرقام. مثال موثّق: خسائر المخزون التالف لا تُرحَّل للحسابات لأنه لا يوجد "سعر تكلفة" لأي صنف في نموذج البيانات — وإدخال رقم مخترع يعني تقديم رقم وهمي كمحاسبة حقيقية (`src/js/accounting.js:9-12`) |
| **قابلية إعادة البيع (White-Label)** | اسم الفندق وشعاره قابلان للتغيير من الإعدادات. كل نسخة مبيعة تحصل على **رمز استرداد فريد** خاص بها — لا يوجد رمز رئيسي مشترك، فتسريب رمز نسخة لا يفتح نسخة أخرى (`src/main.js:44-59`) |
| **بلا اعتماديات خارجية** | لا React، لا jQuery، لا bcrypt. Vanilla JS + وحدة `crypto` المدمجة في Node |

### الهيكل الفندقي المفترض

النظام يولّد عند أول تشغيل هيكلاً افتراضياً:

- **78 غرفة نزلاء** — 3 طوابق × 26 غرفة
  - الطابق 1: الغرف 101 – 126
  - الطابق 2: الغرف 201 – 226
  - الطابق 3: الغرف 301 – 326
- **4 غرف تشغيلية خاصة** على "الدور الأرضي" (الطابق 0)، غير قابلة للحجز إطلاقاً:
  | الرقم | النوع | المعرّف | الدور |
  |---|---|---|---|
  | 1 | مكتب المدير | `sp_manager_office` | إداري |
  | 2 | المقهى | `sp_coffee_shop` | مرتبط بوحدة نقطة البيع |
  | 3 | المغسلة | `sp_laundry` | مرتبط بوحدة المغسلة |
  | 4 | المخزن | `sp_storage` | **مخزونه هو المخزون المركزي للفندق** |

> ملاحظة معمارية مهمة: المخزن ليس بنية بيانات منفصلة — `room.inventory` الخاص بغرفة المخزن **هو نفسه** المستودع المركزي. لا ازدواجية في البيانات.

---

## 2. حالة المشروع الآن — نسختان متوازيتان

هذه نقطة حرجة يجب فهمها قبل أي عمل على الكود.

### 2.1 النسخة الأصلية (Electron Desktop) — مكتملة معمارياً

الكود الأصلي كان تطبيق Electron كاملاً:

```
main.js       (root)  ← العملية الرئيسية: نافذة + نظام ملفات + تشفير
preload.js    (root)  ← جسر آمن عبر contextBridge
src/index.html        ← الواجهة
src/js/*.js           ← 15 وحدة منطق
```

- التخزين: ملف `alaseel-data.json` في `app.getPath('userData')`
- الكتابة **ذرّية** (atomic): يُكتب `.tmp` ثم `rename` — `src/main.js:273-279`
- التشفير: `scrypt` مع `salt` عشوائي 16 بايت ومقارنة `timingSafeEqual` — `src/main.js:24-42`
- عزل كامل: `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true` — `src/main.js:296-302`

### 2.2 النسخة الحالية في المستودع (Web Demo) — تحويل غير مكتمل

سلسلة الـ commits الأخيرة (14 commit) حوّلت المشروع لعرض ويب:

```
commit 2b69a63  chore: move mock files inside src folder
                main.js    → src/main.js
                preload.js → src/preload.js
commit 714a6ac  fix: eliminate Node.js code from preload.js
                ← استُبدل جسر Electron الحقيقي بمحاكاة LocalStorage
... 11 commits إضافية من إصلاحات متتابعة على المحاكاة
```

**ما حدث فعلياً:**

| العنصر | النسخة الأصلية | الحالة الآن |
|---|---|---|
| `preload.js` | جسر `contextBridge` بـ 13 دالة | محاكاة LocalStorage بـ **5 دوال فقط** |
| `main.js` | نقطة دخول Electron | موجود في `src/` لكن **معطّل عملياً** |
| التخزين | ملف JSON + كتابة ذرّية | `localStorage` |
| التشفير | scrypt حقيقي | **لا يوجد — تسجيل الدخول يقبل أي شيء** |
| CSV Export | كتابة ملف حقيقي | **غير موجود** |

**الدوال الخمس المتوفرة في المحاكاة** (`src/preload.js`):
`saveData` · `loadData` · `authSetup` · `authLogin` · `fileExists`

**الدوال الثماني المفقودة** (الكود يستدعيها لكنها غير معرّفة):
`authVerifyRecovery` · `authReset` · `authChangePassword` · `authCreateUser` · `authDeleteUser` · `exportRoomsCsv` · `exportLedgerCsv` · `revealInFolder`

> ⚠️ **النتيجة العملية:** النسخة الحالية **لا تعمل بعد تسجيل الدخول**. التفاصيل الكاملة في [القسم 10](#10-الثغرات-والمشاكل-الحالية).

---

## 3. البنية التقنية والمعمارية

### 3.1 طبقات النظام

```
┌──────────────────────────────────────────────────────────┐
│  Electron Main Process  —  src/main.js                   │
│  • نافذة واحدة (BrowserWindow) + رفض أي نافذة ثانية      │
│  • نظام الملفات: قراءة/كتابة alaseel-data.json          │
│  • التشفير: scrypt للكلمات ورموز الاسترداد               │
│  • تصدير CSV مع BOM لدعم العربية في Excel                │
│  • ترحيل المخطط (migrations) من v1 إلى v11               │
└────────────────────────┬─────────────────────────────────┘
                         │  IPC (ipcMain.handle)
┌────────────────────────┴─────────────────────────────────┐
│  Preload Bridge  —  src/preload.js                       │
│  contextBridge.exposeInMainWorld('alaseelAPI', {...})    │
│  سطح API ضيّق ومُدقَّق — الواجهة لا تلمس Node إطلاقاً     │
└────────────────────────┬─────────────────────────────────┘
                         │  window.alaseelAPI
┌────────────────────────┴─────────────────────────────────┐
│  Renderer  —  src/index.html + src/js/*.js               │
│  ┌────────────────────────────────────────────────────┐  │
│  │ store.js   — نسخة الحالة في الذاكرة + pub/sub      │  │
│  │              حفظ مؤجّل (debounce 250ms)            │  │
│  ├────────────────────────────────────────────────────┤  │
│  │ app.js     — الإقلاع + الشريط الجانبي + الموجّه     │  │
│  ├────────────────────────────────────────────────────┤  │
│  │ util.js    — أدوات مشتركة + موجّه الفواتير (Folio)  │  │
│  ├────────────────────────────────────────────────────┤  │
│  │ i18n.js    — كل النصوص العربية + كتالوج الأصول      │  │
│  ├────────────────────────────────────────────────────┤  │
│  │ 12 وحدة وظيفية (rooms, guests, reservations, ...)  │  │
│  └────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

### 3.2 نمط الوحدة الموحّد

**كل** وحدة في `src/js/` تتبع النمط نفسه بحذافيره:

```javascript
window.Alaseel = window.Alaseel || {};

(function () {
  'use strict';

  const T = () => Alaseel.i18n.<moduleName>;   // النصوص
  const U = Alaseel.util;                       // الأدوات

  let pane, toast;                              // حُقنا عند التركيب
  let view = { mode: 'list' };                  // حالة العرض المحلية
  let draft = null;                             // مسوّدة غير محفوظة

  const Module = {
    mount(contentPaneEl, toastFn, intent) { ... },  // نقطة الدخول
    onSearch(term) { ... }                          // ربط بحث الشريط العلوي
  };

  function data() { return Alaseel.store.get(); }

  // render() → renderList() | renderDetail()
  // كل تعديل: مسّ الحالة مباشرة ثم Alaseel.store.touch()

  Alaseel.<moduleName> = Module;
})();
```

**خصائص هذا النمط:**
- **IIFE** لعزل النطاق — لا متغيرات عامة إلا الكائن المُصدَّر
- **بناء HTML بسلاسل نصية** ثم ربط الأحداث بعد الإدراج — لا مكتبة قوالب
- **الهروب الإلزامي**: كل قيمة يدخلها المستخدم تمر عبر `Alaseel.util.esc()` قبل الحقن
- **نمط المسوّدة**: الكيان الجديد يبقى في الذاكرة (`draft`) حتى الضغط على "حفظ" — الخروج من نموذج نصف مُعبّأ لا يترك أثراً في البيانات

### 3.3 دورة حياة البيانات

```
  المستخدم يعدّل حقلاً
        ↓
  الوحدة تعدّل الكائن مباشرة عبر Alaseel.store.get()
        ↓
  Alaseel.store.touch()
        ↓
  ┌─────────────┬──────────────────────┐
  │  notify()   │   scheduleSave()     │
  │  المشتركون  │   debounce 250ms     │
  └─────────────┴──────────┬───────────┘
                           ↓
             window.alaseelAPI.saveData(state)
                           ↓
               IPC → ipcMain 'store:save'
                           ↓
     كتابة .tmp ثم rename (ذرّية) → alaseel-data.json
```

### 3.4 التوجيه (Routing)

لا يوجد router خارجي. `src/js/app.js:164-191`:

```javascript
function navigateTo(id, opts) {
  activeNav = id;
  if (id === 'guests' && opts) { pendingGuestTabIntent = opts; ... }
  updateActiveNavHighlight();
  document.getElementById('searchInput').value = '';
  renderActiveModule();   // → Alaseel.<module>.mount(contentPane, toast)
}
```

**نظام النوايا (Intents)** — التنقل بين الوحدات مع سياق:

```javascript
// من ملف نزيل → إنشاء حجز له مباشرة
Alaseel.app.navigateTo('guests', {
  tab: 'reservations', action: 'create', guestId: g.guest_id
});
```

عناصر التنقل الأحد عشر (`src/js/app.js:15-27`):
`dashboard` · `rooms` · `guests` · `coffee_shop` · `laundry` · `inventory` · `hr` · `accounting` · `corporate` · `maintenance` · `settings`

> `guests` وحدة **مركّبة**: مدخل واحد في الشريط الجانبي، وتبويبان داخليان (النزلاء / الحجوزات).

---

## 4. نموذج البيانات الكامل

كل شيء داخل كائن JSON واحد. الشكل الافتراضي في `src/main.js:131-178`.

### 4.1 الجذر

```javascript
{
  meta: {
    schema_version: 11,
    installation_id: "<UUID>",        // فريد لكل نسخة مثبّتة
    created_at: "<ISO>"
  },
  hotel: {
    name_ar: "فندق الأصيل",
    logo_dataurl: null,               // data: URL عند رفع شعار مخصص
    logo_variant: "color"             // color | white | black
  },
  auth: { recovery_hash: "<salt:hash>" },
  users: [ /* المستخدمون */ ],
  settings: { theme: "dark" },        // dark | light

  rooms: [ /* 82 غرفة */ ],
  guests: [], reservations: [], reviews: [],

  locationStock: {                    // مواقع افتراضية ليست غرفاً
    lobby: {...}, corridors: {...}, roof: {...},
    workshop: {...}, laundry_transit: {...}
  },
  vouchers: [], wasteLedger: [], inventoryThresholds: {},

  coffeeShop: { categories: [], menu: [], tables: [], orderHistory: [] },
  laundry:    { tiers: [], transactions: [] },

  folioAudit: [], maintenanceTickets: [],
  employees: [], payrollRecords: [], payrollApprovals: {}, shifts: [],
  ledgerEntries: [], nightAuditRuns: [],
  operationalDate: "YYYY-MM-DD",      // التاريخ التشغيلي (يقفله التدقيق الليلي)
  companies: []
}
```

### 4.2 الكيانات بالتفصيل

#### الغرفة `room`
```javascript
{
  room_id: "rm_101",           // sp_storage للغرف الخاصة
  room_number: 101,
  floor: 1,                     // 0 = الدور الأرضي (خاص)
  room_type: "",                // نص حر: "غرفة قياسية"، "جناح ملكي"
  room_model: null,             // a | b | c | null
  capacity: null,               // عدد الأشخاص
  price_per_night: null,
  status: "clean",              // clean|occupied|dirty|maint|oo
  is_special: false,
  special_type: null,           // manager_office|coffee_shop|laundry|storage
  bookable: true,
  inventory: { bed: 0, wardrobe: 0, ... },   // 23 صنفاً
  notes: ""
}
```

**حالات الغرفة الخمس:**
| الحالة | العربية | اللون | يُضبط تلقائياً عند |
|---|---|---|---|
| `clean` | جاهزة | أخضر `#5fa88c` | الافتراضي / بعد التنظيف |
| `occupied` | مشغولة | أزرق `#5485ab` | تسجيل وصول نزيل |
| `dirty` | بحاجة لتنظيف | برتقالي `#dd9d4f` | تسجيل مغادرة / إغلاق تذكرة صيانة |
| `maint` | صيانة | أحمر `#c0566b` | فتح تذكرة صيانة على غرفة شاغرة |
| `oo` | خارج الخدمة | رمادي `#5b6470` | يدوياً فقط |

#### النزيل `guest`
```javascript
{
  guest_id, full_name,          // الاسم الوحيد الإلزامي
  nationality, passport_number, visa_number,
  phone, email, address, profession,
  vip_level: "none",            // none|silver|gold|platinum
  blacklisted: false,
  blacklist_reason: "",
  preferences, emergency_contact, notes,
  created_at, updated_at
}
```

#### الحجز `reservation`
```javascript
{
  reservation_id, guest_id, room_id,
  check_in_date: "YYYY-MM-DD",
  check_out_date: "YYYY-MM-DD",
  arrival_time, adults: 1, children: 0,
  reservation_status: "confirmed",   // confirmed|checked_in|checked_out|cancelled|no_show
  payment_status: "unpaid",          // unpaid|partial|paid|refunded
  total_amount, currency: "EGP",     // EGP|USD|EUR|SAR|AED
  deposit_amount, promo_code, special_requests,
  folio_charges: [ /* الرسوم الإضافية */ ],
  _totalAutoFill: true,              // علم داخلي: هل المبلغ ما زال محسوباً تلقائياً
  created_at, updated_at
}
```

#### رسم الفاتورة `folio_charge`
```javascript
{
  charge_id, source,            // coffee_shop|laundry|maintenance|other
  description, amount, currency,
  transferred_from_room: null,  // يُملأ عند نقل الرسم من غرفة أخرى
  created_at
}
```

#### التقييم `review`
```javascript
{
  review_id, guest_id, room_id, reservation_id,
  score: 1..5,
  category,                     // general|cleanliness|staff|room_condition|noise|amenities
  comment, created_at
}
```

#### السند `voucher` — سجل حركات المخزون
```javascript
{
  voucher_id, type,             // issuance|exchange|maintenance|maintenance_return|waste
  created_at,
  // حقول متغيّرة حسب النوع:
  item, qty, destination, destinationLabel,
  source, sourceLabel, location, locationLabel,
  old_item, old_qty, new_item, new_qty,
  reason, notes
}
```

#### تذكرة الصيانة `maintenanceTicket`
```javascript
{
  ticket_id, location,          // room_id أو lobby|corridors|roof
  category,                     // ac|plumbing|electrical|general
  description, price,
  guest_caused: false,          // إن true → يمكن تحميلها على فاتورة النزيل
  status: "open",               // open|in_progress|resolved
  folio_charge_id: null,        // يُملأ بعد الترحيل للفاتورة
  created_at, resolved_at
}
```

#### الموظف `employee`
```javascript
{
  employee_id, full_name, national_id, role,
  department,                   // front_desk|security|housekeeping
  hired_date, base_salary,
  permissions: [],              // مصفوفة أسماء وحدات (غير مُفعَّلة بعد)
  attendance: [ { date, status } ],   // present|absent|late|leave
  created_at, updated_at
}
```

#### قيد الرواتب `payrollRecord`
```javascript
{
  payroll_id, employee_id,
  month: "YYYY-MM",
  base_salary, tips_allocated, cash_advances, leave_deductions,
  net_pay,                      // = base + tips - advances - deductions
  created_at
}
```

#### الوردية `shift`
```javascript
{
  shift_id, department, employee_id, date,
  start_time: "09:00", end_time: "17:00",
  cost_type: "flat",            // flat | hourly
  cost_amount,
  computed_cost,                // flat → المبلغ | hourly → المبلغ × الساعات
  created_at
}
```

#### قيد دفتر الأستاذ `ledgerEntry`
```javascript
{
  entry_id, type,               // revenue | expense
  source,                       // room|coffee_shop|laundry|maintenance|waste|manual
  description, amount, currency,
  auto: true,                   // true = رُحِّل بالتدقيق الليلي
  reservation_id / ref_id,      // مرجع لمنع الترحيل المكرر
  created_at
}
```

#### الشركة `company`
```javascript
{
  company_id, company_name,
  housing_officer_name, housing_officer_phone,
  accounting_officer_name, accounting_officer_phone,
  fixed_discount: 0,            // مبلغ نقدي ثابت — وليس نسبة
  contract_term: "none",        // none|month|quarter|half_year|season|year
  activation_date, expiration_date,   // الانتهاء محسوب تلقائياً
  company_charges: [], bank_checks: [],
  created_at, updated_at
}
```

#### الشيك البنكي `bank_check`
```javascript
{
  check_id, check_number, bank_name, account_number,
  amount, maturity_date,
  status: "pending",            // pending | paid
  penalty_fees: [ { amount, added_at } ],
  created_at
}
```

### 4.3 كتالوج أصول الغرف — 23 صنفاً ثابتاً

مصدر واحد للحقيقة في `src/js/i18n.js:198-222`، ويُكرَّر في `src/main.js:65-70` لتوليد البيانات الأولية:

| # | Slug | العربية | # | Slug | العربية |
|---|---|---|---|---|---|
| 1 | `bed` | سرير | 13 | `water_heater` | سخان مياه |
| 2 | `wardrobe` | خزانة ملابس | 14 | `bedsheet` | ملاءة سرير |
| 3 | `tv_unit` | وحدة تلفاز | 15 | `mattress` | مرتبة |
| 4 | `armchair` | كرسي بذراعين | 16 | `pillow` | وسادة |
| 5 | `nightstand` | طاولة جانبية | 17 | `mattress_cover` | غطاء مرتبة |
| 6 | `table` | طاولة | 18 | `duvet` | لحاف |
| 7 | `refrigerator` | ثلاجة | 19 | `duvet_cover` | غطاء لحاف |
| 8 | `tv_screen` | شاشة تلفاز | 20 | `towel` | منشفة |
| 9 | `kettle` | غلاية | 21 | `bathroom_mirror` | مرآة حمام |
| 10 | `exhaust_fan` | مروحة شفط | 22 | `curtain` | ستارة |
| 11 | `fan` | مروحة | 23 | `coat_hanger` | علّاقة ملابس |
| 12 | `ac` | مكيف هواء | | | |

### 4.4 نظام ترحيل المخطط (Migrations)

`src/main.js:198-271` — دالة `migrateData()` **نقية وقابلة لإعادة التنفيذ (idempotent)**، تُستدعى عند كل تحميل:

| الإصدار | ما أُضيف |
|---|---|
| v1 → v2 | `meta`, `guests`, `reservations`, `reviews` |
| v2 → v3 | `locationStock`, `vouchers`, `wasteLedger`, `inventoryThresholds` |
| v3 → v4 | `coffeeShop`, `laundry` |
| v4 → v5 | `folioAudit` |
| v5 → v6 | تحويل `auth` أحادي المستخدم إلى مصفوفة `users[]` + `maintenanceTickets` |
| v6 → v7 | `employees`, `payrollRecords`, `payrollApprovals`, `shifts`, `ledgerEntries`, `nightAuditRuns`, `operationalDate` |
| v7 → v8 | `room.room_model` |
| v8 → v9 | `coffeeShop.orderHistory` |
| v9 → v10 | `companies` |
| v10 → v11 | `company.contract_term`, `activation_date`, `expiration_date` |

**حماية ضد الفساد:** إذا فشل تحليل ملف JSON، يُنسخ الملف الأصلي إلى `alaseel-data.json.corrupt-<timestamp>.bak` قبل إنشاء ملف جديد — لا يُدهس أبداً بصمت (`src/main.js:186-191`).

---

## 5. نظام الحماية والحسابات

### 5.1 تشفير كلمات المرور

```javascript
// src/main.js:24-42
function hashSecret(plainText) {
  const salt = crypto.randomBytes(16).toString('hex');
  const derived = crypto.scryptSync(String(plainText), salt, 64).toString('hex');
  return `${salt}:${derived}`;
}

function verifySecret(plainText, stored) {
  const [salt, hash] = stored.split(':');
  const check = crypto.scryptSync(String(plainText), salt, 64).toString('hex');
  return crypto.timingSafeEqual(Buffer.from(hash,'hex'), Buffer.from(check,'hex'));
}
```

- **scrypt** — خوارزمية مقاومة للعتاد المتخصص (ASIC-resistant)
- **salt عشوائي 16 بايت** لكل كلمة مرور
- **`timingSafeEqual`** — مقارنة بزمن ثابت تمنع هجمات التوقيت
- **بدون اعتماديات خارجية** — `crypto` المدمجة في Node فقط

### 5.2 رمز الاسترداد

```javascript
// src/main.js:48-59
const alphabet = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789';  // بلا 0/O/1/I
// النتيجة: XXXX-XXXX-XXXX-XXXX  (16 خانة، 4 مجموعات)
```

**خصائص التصميم:**
- الأبجدية تستبعد الأحرف الملتبسة بصرياً (`0`/`O`, `1`/`I`) — تقليل أخطاء النسخ اليدوي
- يُولَّد **مرة واحدة** عند الإعداد الأول، ويُعرض **مرة واحدة فقط**
- يُخزَّن كـ hash فقط — لا يمكن استرجاعه من الملف
- **فريد لكل نسخة مثبّتة** — لا يوجد رمز رئيسي (master token) مشترك

### 5.3 تدفقات المصادقة الخمسة

كلها تُرسم داخل عنصر `#authScreen` الواحد — `src/js/auth.js`:

```
1. الإعداد الأول (renderSetup)
   • يظهر فقط عندما تكون users[] فارغة
   • التحقق: اسم ≥ 3 أحرف، كلمة مرور ≥ 6 أحرف، تطابق التأكيد
   • أول مستخدم دائماً role='admin'
        ↓
2. كشف رمز الاسترداد (renderRecoveryReveal)
   • عرض لمرة واحدة + زر نسخ + تأكيد إجباري "لقد حفظت الرمز"
        ↓
3. تسجيل الدخول (renderLogin)
   • Enter في حقل كلمة المرور = إرسال
   • رابط "نسيت كلمة المرور؟"
        ↓
4. إدخال رمز الاسترداد (renderRecoveryEntry)
   • تطبيع الحروف لأحرف كبيرة قبل المقارنة
        ↓
5. إعادة تعيين إجبارية (renderForcedReset)
   • ⚠️ لا يوجد زر "رجوع" — الإعادة إلزامية بعد التحقق الناجح
   • تستبدل حساب المسؤول فقط، ولا تمس حسابات الموظفين
   • تُصدر رمز استرداد جديداً
```

### 5.4 الأدوار والصلاحيات

**الأدوار في `users[]`:** `admin` وأدوار أخرى مشار إليها في تعليقات الكود (Receptionist / Barista / Laundry / Accountant).

**الحماية المنفَّذة:**
- `auth:deleteUser` يرفض حذف آخر مسؤول — `LAST_ADMIN` (`src/main.js:469-472`)
- كلمات المرور تُتحقَّق في العملية الرئيسية، لا في الواجهة
- `will-navigate` يمنع الخروج من صفحة التطبيق (`src/main.js:318-323`)

**غير المنفَّذ:**
- ❌ لا توجد **واجهة** لإنشاء أو حذف المستخدمين — دالتا IPC `auth:createUser` و `auth:deleteUser` موجودتان في `main.js` لكن **لا يستدعيهما أي كود واجهة**
- ❌ حقل `employee.permissions[]` يُحفظ لكن **لا شيء يفرضه** — أي مستخدم مسجّل يرى كل الوحدات

---

## 6. الوحدات المنفّذة — تفصيل كامل

### 6.1 لوحة المعلومات — `src/js/app.js:222-245`

**المنفَّذ:** شريط 5 مؤشرات أداء محسوبة لحظياً من مصفوفة الغرف:

| المؤشر | المعادلة |
|---|---|
| نسبة الإشغال | `round(occupied / bookable × 100)` |
| غرف جاهزة | عدد `status === 'clean'` من القابلة للحجز |
| بحاجة لتنظيف | عدد `status === 'dirty'` من القابلة للحجز |
| قيد الصيانة | عدد `status === 'maint'` (كل الغرف) |
| إجمالي الغرف | طول المصفوفة |

**غير المنفَّذ:** نصوص تنبيهات الشيكات المتأخرة موجودة في `i18n.dashboard` والدالة `Corporate.overdueChecks()` جاهزة — لكن اللوحة **لا تعرضها**. راجع [القسم 10](#10-الثغرات-والمشاكل-الحالية).

---

### 6.2 وحدة الغرف — `src/js/rooms.js` (474 سطراً)

#### عرض الشبكة

- **بلاطات ملوّنة** حسب الحالة، مرتّبة بالطابق ثم الرقم (الدور الأرضي يُعرض أخيراً)
- **فلاتر الطوابق:** الكل / 1 / 2 / 3 / غرف خاصة
- **فلاتر النماذج:** الكل / نموذج أ / نموذج ب / نموذج ج / بدون نموذج
- **بحث حي** من الشريط العلوي: رقم الغرفة، النوع، اسم الغرفة الخاصة
- **مفتاح ألوان (Legend)** للحالات الخمس
- **علامة تحذير ⚠** على البلاطة عند رصد تقييمات سلبية متكررة

#### عرض التفاصيل

عند النقر على غرفة، يُستبدَل محتوى اللوحة (لا نافذة منبثقة):

- تعديل: رقم الغرفة، الطابق، النموذج، النوع، السعة، السعر، الحالة، الملاحظات
- **محرّر المخزون:** 23 صنفاً بأزرار +/− وإجمالي حي
- **شريط الرضا:** متوسط التقييم + عدد التقييمات + تحذير عند الرصد السلبي
- **الحذف بتأكيد داخل الصفحة** — منطقة الحذف معزولة في `wireDeleteZone()` حتى لا يتضاعف ربط زر الحفظ عند إعادة الرسم (`src/js/rooms.js:447-471`)

#### قيود الغرف الخاصة

- رقم الغرفة والطابق **معطَّلان**
- لا يوجد نوع/سعة/سعر (لا تُدر إيرادات ضيافة)
- الحالة `occupied` غير متاحة
- **لا يمكن حذفها**
- شريط تنبيهي يشرح دورها التشغيلي

#### تصدير CSV

`src/main.js:488-517` — يكتب في `~/Documents/Alaseel-PMS-Exports/rooms-inventory-YYYY-MM-DD.csv`

- **BOM (`﻿`)** في بداية الملف حتى يعرض Excel العربية صحيحة
- أعمدة: رقم الغرفة، الطابق، النوع، الحالة، السعة، السعر + 23 عمود صنف + إجمالي القطع
- نهايات أسطر `\r\n` (توافق Windows)
- زر "إظهار في المجلد" في رسالة النجاح

---

### 6.3 وحدة النزلاء — `src/js/guests.js` (330 سطراً)

#### القائمة
- أفاتار بالحرف الأول بـ 5 ألوان مشتقة من `hash(guest_id)` — لون ثابت لكل نزيل
- أعمدة: الاسم، الجنسية، الهاتف، فئة VIP، شارة الحظر
- ترتيب أبجدي عربي (`localeCompare(…, 'ar')`)
- بحث عبر: الاسم، الجنسية، الهاتف، رقم الجواز

#### ملف النزيل
14 حقلاً: الاسم الكامل (إلزامي) · الجنسية · رقم الجواز · رقم التأشيرة · الهاتف · البريد · العنوان · المهنة · فئة VIP · قائمة الحظر + السبب · التفضيلات · جهة اتصال الطوارئ · الملاحظات

- **فئات VIP:** عادي / فضي / ذهبي / بلاتيني — بشارات ملوّنة
- **قائمة الحظر:** تفعيلها يُظهر حقل السبب، ويظهر تحذير أحمر عند حجز هذا النزيل
- **سجل الإقامات:** كل الحجوزات مرتّبة بالأحدث مع الغرفة والتواريخ والحالة
- **سجل التقييمات:** النجوم + الغرفة + التصنيف + التعليق
- **زر "حجز جديد لهذا النزيل"** → ينتقل لتبويب الحجوزات بمسوّدة جاهزة

> **قاعدة حفظ السجل:** حذف نزيل **لا يحذف** حجوزاته أو تقييماته — تبقى كسجل تاريخي (منصوص عليها في نص التأكيد).

---

### 6.4 وحدة الحجوزات — `src/js/reservations.js` (782 سطراً — الأكبر)

#### القائمة
- فلاتر الحالة: الكل / قادمة / داخل الفندق / غادرت / ملغاة / لم يحضر
- أعمدة: النزيل (+ شارة حظر) · الغرفة · الوصول · المغادرة · حالة الحجز · حالة الدفع
- ترتيب بأحدث تاريخ وصول

#### نموذج الحجز — تخطيط عمودين

**العمود الرئيسي:**

1. **بيانات النزيل** — منتقي بحث حي (اسم/هاتف/جواز، حد 8 نتائج) + رابط "أضف نزيلاً جديداً"
   > النزيل **يُقفل** بعد حفظ الحجز — لا يمكن تغييره لاحقاً (قرار تصميمي صريح: `src/js/reservations.js:250`)

2. **بيانات الغرفة** — قائمة الغرف القابلة للحجز فقط، مع **وسم التعارض** بجوار أي غرفة محجوزة في نفس الفترة

3. **تفاصيل الإقامة** — تاريخ الوصول · المغادرة · وقت الوصول المتوقع · البالغون · الأطفال

4. **التفاصيل المالية** — المبلغ · العملة (5 عملات) · العربون · كود الخصم

5. **الطلبات الخاصة**

**العمود الجانبي:** ملخص حي لتقييمات النزيل السابقة — يُملأ لحظة اختياره (المتوسط + النجوم + آخر 5 تقييمات).

#### منطق أساسي

**الحساب التلقائي للمبلغ** (`src/js/reservations.js:704-717`):
```javascript
// عدد الليالي × سعر الغرفة — لكن فقط طالما لم يكتب المستخدم قيمة بنفسه
if (!target._totalAutoFill) return;   // العلم ينطفئ عند أول إدخال يدوي
```
> السبب: سعر متفاوَض عليه أو إقامة مجانية يجب ألّا يُدهس بصمت.

**كشف التعارض** (`src/js/util.js:34-38` + `reservations.js:719-725`):
```javascript
// اختبار تداخل فترتين نصف مفتوحتين: [aStart,aEnd) vs [bStart,bEnd)
return as < be && bs < ae;
// يُحتسب التعارض فقط مع حجوزات confirmed أو checked_in
```
> المغادرة في نفس يوم وصول نزيل آخر **ليست** تعارضاً — سلوك فندقي صحيح.

**مزامنة الحقول قبل إعادة الرسم** (`syncFieldsToModel`, سطر 152-169):
> اختيار نزيل أو تغيير غرفة يعيد بناء النموذج كاملاً. تُلتقط قيم كل الحقول المكتوبة إلى الكائن **أولاً**، وإلا ضاعت بصمت.

#### دورة حياة الحجز

```
     confirmed ──[تسجيل وصول]──> checked_in ──[تسجيل مغادرة]──> checked_out
         │                            │                              │
    [إلغاء]                     الغرفة → occupied              الغرفة → dirty
         ↓                       الفاتورة مفتوحة              الفاتورة تُقفل
     cancelled                                                       ↓
                                                          نافذة التقييم السريع
```

**قواعد صارمة:**
- **لا مغادرة قبل السداد** — يجب أن تكون حالة الدفع `paid` أو `refunded`، وإلا يُرفض الإجراء (`src/js/reservations.js:613-617`)
- الحجوزات `checked_out` و `cancelled` **مقفلة** — كل الحقول معطَّلة
- تحذير أحمر يظهر على حجز `checked_in` غير مسدَّد

#### الفاتورة (Folio)

- **رسوم الغرفة** (المبلغ الإجمالي) + كل الرسوم الإضافية + **الإجمالي الشامل**
- **إضافة رسم يدوي** — متاح فقط أثناء `checked_in`
- **نقل رسم لغرفة أخرى**:
  - سبب **إلزامي**
  - يجب أن تحتوي الغرفة الهدف على نزيل مسجّل دخول
  - يُنشأ `charge_id` جديد مع `transferred_from_room`
  - يُسجَّل قيد تدقيق كامل في `folioAudit`: من/إلى/المبلغ/السبب/بواسطة/متى

> **الفاتورة بلا حالة إضافية:** الفاتورة "مفتوحة" طالما وُجد حجز `checked_in` لتلك الغرفة. بعد المغادرة يتوقف البحث عن إيجادها تلقائياً — لا حاجة لعلم `is_open` (`src/js/util.js:83-90`).

#### التقييم عند المغادرة
نافذة اختيارية تظهر داخل الصفحة بعد المغادرة: 5 نجوم تفاعلية + تصنيف (عام/النظافة/الخدمة/حالة الغرفة/الضوضاء/المرافق) + تعليق + زر تخطي.

---

### 6.5 وحدة المخزون والسندات — `src/js/inventory.js` (450 سطراً)

#### مفهوم المواقع

| النوع | أمثلة | التخزين |
|---|---|---|
| **غرف حقيقية** | 101، 205، المخزن، المقهى | `room.inventory` |
| **مواقع افتراضية** | اللوبي، الممرات، السطح، الورشة، المغسلة الخارجية | `data.locationStock[key]` |

الدالة `Alaseel.util.getStockBucket(locationKey)` تُرجع الكائن الحي للتعديل المباشر (`src/js/util.js:52-61`).

#### التبويبات المنفَّذة

**① نظرة عامة**
- جدول المخزون المركزي: الصنف · الكمية في المخزن · **الحد الأدنى (قابل للتعديل)** · الموزّع على الغرف
- **شريط تنبيه النقص** أعلى الصفحة عند وجود أصناف تحت الحد
- تلوين أحمر للكميات المنخفضة
- **قسم الورشة**: الأصناف قيد الإصلاح + زر "استرجاع إلى المخزن"

**② سند صرف (Issuance)**
```
المخزن ──[الصنف × الكمية]──> الوجهة (غرفة / لوبي / ممرات / سطح)
```
- تحقق: الكمية المطلوبة ≤ المتاح
- تنبيه لحظي إذا عبر هذا الصرف الحدّ الأدنى (`crossedThreshold`)

**③ سند تبديل (Exchange)** — أربع حركات في عملية واحدة
```
الموقع ──[الصنف القديم]──> المخزن   (يعود صالحاً للاستخدام)
المخزن ──[الصنف الجديد]──> الموقع
```

**④ سند صيانة (Maintenance)**
```
أي موقع ──[الصنف]──> الورشة
```
الاسترجاع من تبويب "نظرة عامة" (نوع سند `maintenance_return`).

**⑤ سند تالف/فاقد (Waste)**
```
أي موقع ──[الصنف]──> ✗ خارج النظام نهائياً
```
- **سبب إلزامي** — لا يُقبل السند بدونه
- يُسجَّل في `wasteLedger` **و** في `vouchers`
- **لا يعود لأي مخزون**
- زر أحمر (`btn-danger`) تمييزاً للخطورة

**⑥ سجل الحركات**
- كل السندات بالترتيب الزمني العكسي مع وصف مقروء لكل نوع
- سجل التالف والفاقد منفصل بلون تحذيري

> ❌ **تبويب "سند استلام" (Intake) غير منفَّذ** — النصوص موجودة كاملة في `i18n.inventory.tabs.intake` وأزرار التبويب تُبنى من `Object.keys(T().tabs)`، لكن `renderActiveTab()` لا يحتوي فرعاً له → **لوحة فارغة**. النتيجة: **لا توجد طريقة لإضافة مخزون جديد وارد من مورّد** إلا بالتعديل اليدوي لمخزون غرفة المخزن من وحدة الغرف.

---

### 6.6 وحدة المقهى (نقطة البيع) — `src/js/coffeeshop.js` (408 أسطر)

#### تبويب الطاولات
- بطاقات طاولات مع الاسم والحالة والإجمالي الجاري
- إنشاء طاولة باسم حر ("طاولة 1"، "شرفة 2")
- **تفاصيل الطاولة**: قائمة الأصناف على اليمين، السلة والتسوية على اليسار
- تعديل الكميات بأزرار +/− (الوصول للصفر يحذف الصنف)
- حذف الطاولة متاح فقط وهي فارغة

#### طرق التسوية الثلاث

| الطريقة | الوجهة | التحقق |
|---|---|---|
| **ترحيل لفاتورة غرفة** | `folio_charges` للحجز النشط | الغرفة موجودة + بها نزيل `checked_in` |
| **ترحيل لفاتورة شركة** | `company.company_charges` | الشركة موجودة + **العقد غير منتهٍ** |
| **تحصيل نقدي** | يُرحَّل بالتدقيق الليلي | — |

> **لا يمكن تجاوز التسوية:** طاولة بها أصناف لا يمكن حذفها ولا تركها معلّقة بلا إغلاق (`i18n.coffeeShop.settle_hint`).

#### الأرشفة وإعادة التعيين
`archiveAndResetTable()` (`src/js/coffeeshop.js:224-239`):
1. يُدفع الطلب المكتمل إلى `orderHistory` (بالإجمالي وطريقة التسوية والمرجع)
2. تُفرَّغ الطاولة وتُعاد فوراً لحالة `open` جاهزة للاستخدام

> السبب: طاولة فيزيائية لا "تُغلق" — الزبون يغادر والطاولة تستقبل غيره فوراً.

#### تبويب القائمة
- إدارة الفئات (إضافة/حذف)
- حذف فئة يحذف كل أصنافها (بتأكيد صريح)
- إدارة الأصناف داخل كل فئة (اسم + سعر)

> ❌ **تبويب "سجل الطلبات" غير معروض** — النصوص موجودة (`i18n.coffeeShop.tabs.history`, `order_history_empty`) والبيانات تُجمَّع فعلاً في `orderHistory`، لكن `render()` يبني تبويبَي `tables` و `menu` فقط يدوياً (`src/js/coffeeshop.js:34-37`).

---

### 6.7 وحدة المغسلة — `src/js/laundry.js` (282 سطراً)

نموذج مختلف عن المقهى: **لا طاولات مفتوحة** — ابنِ السلة، سوِّ الحساب فوراً، انتهى.

**التبويبات:**

1. **معاملة جديدة** — بطاقات الأصناف والأسعار + سلة + تسوية (فاتورة غرفة / نقدي)
2. **الأصناف والأسعار** — إدارة فئات التسعير:
   - اسم الصنف (قميص، بدلة، …)
   - نوع المعالجة: **غسيل** / **تنظيف جاف** / **كي**
   - السعر
3. **سجل المعاملات** — كل المعاملات مع الطريقة ورقم الغرفة والإجمالي

> يستخدم **نفس** موجّه الفواتير `postFolioCharge` المستخدم في المقهى — منطق واحد، لا تكرار.
>
> ❌ لا يدعم الترحيل لفاتورة شركة (المقهى يدعمه).

---

### 6.8 وحدة الصيانة — `src/js/maintenance.js` (301 سطر)

#### التذاكر
- **الموقع:** أي غرفة (بما فيها الخاصة) أو لوبي/ممرات/سطح
- **التصنيف:** تكييف · سباكة · كهرباء · صيانة عامة
- **الوصف** و **التكلفة**
- **علم "بسبب النزيل"** — يفتح مسار التحميل على الفاتورة
- **الحالة:** مفتوحة → قيد التنفيذ → تم الحل (مع إمكانية إعادة الفتح)

#### الأتمتة على حالة الغرفة

```javascript
// src/js/maintenance.js:55-63
function applyRoomMaintenanceStatus(locationKey) {
  const room = findRoom(locationKey);
  if (!room || room.status === 'occupied') return;   // ← لا نمسّ غرفة بها نزيل
  room.status = 'maint';
}
function releaseRoomFromMaintenance(locationKey) {
  if (room && room.status === 'maint') room.status = 'dirty';  // فحص وتنظيف قبل الجاهزية
}
```

| الحدث | التأثير |
|---|---|
| فتح تذكرة على غرفة **شاغرة** | الغرفة → `maint` |
| فتح تذكرة على غرفة **مشغولة** | ⚠️ **لا تغيير** — النزيل لا تُسحب غرفته من تحته |
| إغلاق التذكرة | `maint` → `dirty` (وليس `clean`) |
| إعادة فتح التذكرة | الغرفة → `maint` مجدداً |

#### التحميل على الفاتورة
يظهر قسم الفوترة فقط عند: `guest_caused = true` **و** الموقع غرفة حقيقية **و** التكلفة محدَّدة **و** لم يُحمَّل سابقاً. عند النجاح يُخزَّن `folio_charge_id` لمنع التكرار.

---

### 6.9 وحدة الموظفين والرواتب — `src/js/hr.js` (537 سطراً)

#### تبويب دليل الموظفين
- الاسم · الرقم القومي · الوظيفة · القسم · تاريخ التعيين · الراتب الأساسي
- **الأقسام:** الاستقبال · الأمن · التدبير المنزلي
- **مربّعات الصلاحيات** لـ 9 وحدات (تُحفظ، غير مُفعَّلة)
- **الحضور والانصراف:**
  - زر "تسجيل حضور اليوم" (يختفي بعد التسجيل)
  - إدخال يدوي بتاريخ وحالة
  - الحالات: حاضر · غائب · متأخر · إجازة
  - قيد واحد لكل يوم (التكرار يستبدل السابق)

#### تبويب حساب الرواتب

```
صافي الراتب = الراتب الأساسي + البقشيش − السُلف − الاستقطاعات
```

- منتقي شهر (`<input type="month">`)
- جدول لكل الموظفين بحساب حي للصافي عند كل تعديل
- **حفظ مسوّدة** أو **اعتماد**
- الاعتماد **يقفل** كل حقول الشهر (شارة "معتمد")
- إمكانية إلغاء الاعتماد للتصحيح

> **قرار تصميمي معلن:** الاستقطاعات **تُدخل يدوياً دائماً** ولا تُشتق من سجل الحضور تلقائياً (`i18n.hr.deductions_manual_hint`) — لأن قواعد الخصم تختلف من فندق لآخر وأي اشتقاق تلقائي سيكون افتراضاً مخترعاً.

#### تبويب جدولة الورديات
- شبكة أسبوعية بتنقل (الأسبوع السابق/التالي)
- تصفية بالقسم
- إضافة وردية: الموظف · التاريخ · البداية · النهاية · نوع التكلفة · المبلغ
- **نوعا التكلفة:**
  - `flat` — مبلغ ثابت للوردية
  - `hourly` — المبلغ × عدد الساعات (مع معالجة الورديات العابرة لمنتصف الليل: `if (mins <= 0) mins += 24*60`)
- **إجمالي تكلفة الأسبوع** محسوباً — جاهز للربط بدفتر الأستاذ

---

### 6.10 وحدة الحسابات والتدقيق الليلي — `src/js/accounting.js` (310 أسطر)

#### تبويب نظرة عامة
مؤشرات الإيرادات/المصروفات/صافي الربح **مجمَّعة حسب العملة** — لا يُجمع الجنيه مع الدولار في رقم واحد وهمي.

#### تبويب دفتر الأستاذ العام
- إضافة قيد يدوي (إيراد/مصروف + مصدر + مبلغ + وصف)
- شارة **تلقائي** / **يدوي** على كل قيد
- تصدير CSV كامل

#### تبويب التدقيق الليلي — قلب النظام المحاسبي

`runNightAudit()` (`src/js/accounting.js:224-275`) يرحّل **فقط ما يمكن حسابه بصدق**:

| # | المصدر | ما يُرحَّل | مفتاح منع التكرار |
|---|---|---|---|
| 1 | حجوزات `checked_out` | **الإجمالي الكامل للفاتورة** (الغرفة + كل الإضافات) | `reservation_id` |
| 2 | طلبات المقهى المسوّاة **نقداً أو لشركة** | إجمالي الطلب | `ref_id = order_id` |
| 3 | معاملات المغسلة **النقدية** | إجمالي المعاملة | `ref_id = transaction_id` |
| 4 | تذاكر صيانة `resolved` **غير** ناتجة عن النزيل | التكلفة (كمصروف) | `ref_id = ticket_id` |

**قواعد الحماية من الازدواج:**

> **① الفاتورة هي المكان الوحيد لعدّ إيراد الإقامة.** طلب مقهى رُحِّل لفاتورة غرفة يُحتسب ضمن إجمالي تلك الفاتورة عند المغادرة — ولا يُعدّ مرة ثانية كإيراد مقهى.
>
> **② كل قيد تلقائي له مرجع ثابت** — إعادة تشغيل التدقيق لا تضاعف أي مبلغ.
>
> **③ لا يمكن تشغيل التدقيق مرتين لنفس اليوم التشغيلي** (`nightAuditRuns` تُفحص أولاً).
>
> **④ خسائر المخزون التالف لا تُرحَّل كمصروف** — لا يوجد سعر تكلفة لأي صنف في نموذج البيانات، واختراع رقم يعني تقديم رقم وهمي كمحاسبة (`src/js/accounting.js:9-12`).

**إقفال اليوم:** بعد الترحيل يتقدّم `operationalDate` يوماً واحداً ويُسجَّل قيد في `nightAuditRuns` بعدد القيود المُنشأة.

#### تبويب سجل تصحيحات الفواتير
عرض للقراءة فقط لكل عمليات نقل الرسوم: من غرفة · إلى غرفة · المبلغ · السبب · بواسطة · التاريخ.

---

### 6.11 وحدة الشركات — `src/js/companies.js` (367 سطراً)

**شريحة مصغّرة (Minimal Slice)** بحسب تعليق الملف نفسه — الهدف: تمكين ترحيل رسوم المقهى/المغسلة لحساب شركة بدل غرفة نزيل.

#### سجل الشركة
- اسم الشركة (إلزامي)
- مسؤول الإسكان + هاتفه
- مسؤول الحسابات + هاتفه
- **الخصم الثابت** — مبلغ نقدي وليس نسبة (منصوص عليه صراحة)

#### إدارة العقد
| المدة | الأشهر |
|---|---|
| بدون عقد طويل الأجل | — |
| شهر | 1 |
| ربع سنة | 3 |
| **موسم** | **3** |
| نصف سنة | 6 |
| سنة | 12 |

> **"الموسم" = 3 أشهر** — نفس مدى الربع، وهو العرف الفندقي الشائع لكتلة موسم مرتفع/منخفض. مذكور صراحة لأن المصطلح ليس له طول ثابت عالمي (`src/js/companies.js:195-198`).

- تاريخ الانتهاء **محسوب تلقائياً** من تاريخ التفعيل + المدة (الحقل معطَّل للتحرير)
- شارة **"العقد منتهٍ"** عند تجاوز التاريخ
- **العقد المنتهي يمنع ترحيل رسوم جديدة** — `postCompanyCharge` يرجع `CONTRACT_EXPIRED`

#### الرسوم المرحّلة
عرض كل الرسوم القادمة من المقهى + الإجمالي.

#### الشيكات البنكية
- رقم الشيك · البنك · رقم الحساب · المبلغ · تاريخ الاستحقاق
- الحالة: قيد الانتظار / مدفوع
- **حساب أيام التأخير** تلقائياً لكل شيك متأخر مع عرض أحمر
- ترتيب بتاريخ الاستحقاق

#### دوال جاهزة بلا واجهة ⚠️
| الدالة | الوظيفة | الحالة |
|---|---|---|
| `postCompanyCharge()` | ترحيل رسم للشركة | ✅ مستخدمة من المقهى |
| `overdueChecks()` | كل الشيكات المتأخرة | ❌ **لا واجهة تستدعيها** |
| `expiredContracts()` | كل العقود المنتهية | ❌ **لا واجهة تستدعيها** |
| `markCheckPaid()` | تحديد شيك كمدفوع | ❌ **لا زر في الواجهة** |
| `addPenalty()` | إضافة ضريبة تأخير | ❌ **لا زر في الواجهة** (النصوص موجودة) |

---

### 6.12 وحدة الإعدادات — `src/js/settings.js` (162 سطراً)

#### بيانات الفندق (White-Label)
- **اسم الفندق** — يظهر في الشريط الجانبي وشاشة الدخول
- **الشعار:**
  - 3 نسخ مرفقة: ملوّن / أبيض (للخلفيات الداكنة) / أسود (للطباعة)
  - **رفع شعار مخصص** → يُحوَّل لـ `data:` URL ويُخزَّن داخل ملف البيانات
  - معاينة حية قبل الحفظ

#### أمان الحساب
تغيير اسم المستخدم وكلمة المرور بعد التحقق من كلمة المرور الحالية.

> ⚠️ هذا القسم **معطوب حالياً** — راجع [القسم 10.4](#104-قسم-الأمان-في-الإعدادات-معطوب).

---

## 7. التدفقات المشتركة بين الوحدات

### 7.1 موجّه الفواتير (Folio Router)

المكان **الوحيد** الذي يُرحَّل منه أي رسم لفاتورة نزيل — `src/js/util.js:91-111`:

```javascript
postFolioCharge(roomNumber, charge)
```

```
     ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
     │    المقهى    │   │   المغسلة    │   │   الصيانة    │
     └──────┬───────┘   └──────┬───────┘   └──────┬───────┘
            └──────────────────┼──────────────────┘
                               ↓
                 Alaseel.util.postFolioCharge(roomNumber, ...)
                               ↓
              ابحث عن غرفة برقمها (غير خاصة)  →  ROOM_NOT_FOUND
                               ↓
       ابحث عن حجز reservation_status === 'checked_in'  →  NO_ACTIVE_GUEST
                               ↓
                  أضف الرسم إلى reservation.folio_charges
                               ↓
                        Alaseel.store.touch()
```

**لماذا رقم الغرفة وليس `room_id`؟** لأن الموظف يفكّر برقم الغرفة، لا بمعرّف داخلي.

**أكواد الأخطاء:** `ROOM_NOT_FOUND` · `NO_ACTIVE_GUEST` · `REASON_REQUIRED` · `SAME_ROOM` · `SOURCE_NOT_FOUND` · `CHARGE_NOT_FOUND`

### 7.2 دورة حياة المخزون

```
     [❌ سند استلام — غير منفَّذ]
                 ↓
        ┌────────────────┐
        │     المخزن     │◄──────── استرجاع من الورشة
        │ (المخزون المركزي)│◄──────── الصنف القديم من سند التبديل
        └───┬────────┬───┘
     صرف →  │        │  ← تبديل (الصنف الجديد)
            ↓        ↓
      ┌──────────────────────┐
      │  الغرف / اللوبي /     │────[سند صيانة]───► الورشة
      │  الممرات / السطح      │
      └──────────┬───────────┘
                 │
            [سند تالف/فاقد]
                 ↓
        ✗ خارج النظام نهائياً (مع سبب إلزامي)
```

### 7.3 مسار الإيراد الكامل

```
حجز → تسجيل وصول (الغرفة occupied، الفاتورة مفتوحة)
   │
   ├─ طلب مقهى ──[ترحيل للغرفة]──► folio_charges
   ├─ معاملة مغسلة ─[ترحيل للغرفة]─► folio_charges
   ├─ صيانة بسبب النزيل ──────────► folio_charges
   └─ رسم يدوي ──────────────────► folio_charges
   │
تسوية الدفع (paid / refunded) ← ⛔ إلزامية
   │
تسجيل مغادرة → الغرفة dirty، الفاتورة تُقفل، نافذة التقييم
   │
التدقيق الليلي → قيد إيراد واحد بالإجمالي الكامل → دفتر الأستاذ
```

**المسار الموازي (بلا فاتورة غرفة):**
```
مقهى/مغسلة → تحصيل نقدي → orderHistory / transactions
                                    ↓
                          التدقيق الليلي → قيد إيراد
```

---

## 8. واجهة المستخدم ونظام التصميم

### 8.1 الثيمات

مبنية على متغيّرات CSS مع تبديل عبر `html[data-theme]` — `src/styles/main.css:8-58`.

**الوضع الداكن (الافتراضي):**
| الدور | القيمة |
|---|---|
| الخلفية | `#14181d` |
| السطح | `#1b2128` |
| السطح المرتفع | `#222a33` |
| النص | `#ece7dd` |
| اللون المميّز | `#c9a227` (ذهبي) |

**الوضع الفاتح:**
| الدور | القيمة |
|---|---|
| الخلفية | `#f6f4ee` |
| السطح | `#ffffff` |
| النص | `#22262c` |
| اللون المميّز | `#c9a227` (نفسه) |

**ألوان الحالات** (ثابتة دلالياً في الوضعين، تتغيّر درجاتها الخلفية فقط):

| الحالة | داكن | فاتح |
|---|---|---|
| جاهزة | `#5fa88c` | `#3f8567` |
| مشغولة | `#5485ab` | `#356690` |
| بحاجة لتنظيف | `#dd9d4f` | `#b97a2e` |
| صيانة | `#c0566b` | `#a3324a` |
| خارج الخدمة | `#5b6470` | `#4a525c` |

### 8.2 الخطوط
| الخط | الاستخدام |
|---|---|
| **IBM Plex Sans Arabic** (400/500/600/700) | النصوص العامة |
| **Fraunces** (500/600/700) | العناوين |
| **IBM Plex Mono** (500/600) | الأرقام ورمز الاسترداد |

### 8.3 المكوّنات المشتركة
`kpi-strip` · `panel` / `panel-head` / `panel-title` · `room-tile` · `list-row` · `chip` · `status-badge` · `field-group` / `field-input` · `stepper` · `inline-form` · `inline-confirm` · `toast` · `booking-layout` (عمودان) · `sidebar-panel` · `empty-note` · `notice-banner` · `back-link`

### 8.4 نظام التنبيهات (Toast)
`src/js/app.js:260-274` — عنصر واحد في أسفل الصفحة:
- رسالة عادية: 2400 مللي ثانية
- رسالة بزر إجراء أو `long`: 5000 مللي ثانية
- زر إجراء اختياري (مثل "إظهار في المجلد" بعد التصدير)

### 8.5 الأيقونات
كل الأيقونات **SVG مضمّنة** مرسومة بأشكال أوّلية فقط (مستطيلات، دوائر، خطوط، أقواس بسيطة) — لا ملفات أيقونات خارجية ولا منحنيات حرة.

### 8.6 دعم RTL
- `dir="rtl"` على مستوى الوثيقة
- `.ltr-num` لعرض الأرقام والتواريخ والهواتف باتجاه LTR داخل نص عربي
- `.rtl-flip` لعكس أسهم التنقل
- التواريخ بصيغة `ar-EG-u-nu-latn` — أسماء عربية بأرقام لاتينية
- الساعة بنظام 12 ساعة مع ص/م

---

## 9. خريطة الملفات ومسؤولياتها

```
ALASSEL-PMS-/
├── package.json                 39 سطراً    إعدادات Electron Builder (NSIS/Windows)
├── package-lock.json                        قفل الاعتماديات
├── .gitignore                               node_modules, dist, .env, *.log, .DS_Store
├── DOCUMENTATION.md                         ← هذا الملف
│
├── assets/
│   ├── logo/  logo-black.png · logo-color.png · logo-white.png
│   └── icons/ icon.png · icon.ico
│
└── src/
    ├── main.js                 539 سطراً    عملية Electron الرئيسية
    │                                        • نافذة واحدة + رفض النوافذ الثانية
    │                                        • تشفير scrypt + رموز الاسترداد
    │                                        • قراءة/كتابة ذرّية لملف JSON
    │                                        • 11 مستوى ترحيل مخطط
    │                                        • 12 معالج IPC
    │                                        • تصدير CSV (غرف + دفتر أستاذ)
    │
    ├── preload.js               83 سطراً    ⚠️ حالياً: محاكاة LocalStorage للويب
    │                                        (الأصل: جسر contextBridge بـ 13 دالة)
    │
    ├── index.html               80 سطراً    الهيكل: #authScreen + #appShell + #toast
    │                                        وترتيب تحميل الـ 15 سكربت
    │
    ├── styles/
    │   ├── main.css            401 سطر      المتغيّرات · الثيمات · الهيكل · المكوّنات
    │   └── extended.css        202 سطر      مكوّنات المراحل 2-4
    │
    └── js/
        ├── i18n.js             821 سطراً    كل النصوص العربية + كتالوج 23 صنفاً
        ├── util.js             168 سطراً    esc · genId · stars · fmtDate ·
        │                                    datesOverlap · getStockBucket ·
        │                                    roomSentiment · postFolioCharge ·
        │                                    transferFolioCharge
        ├── store.js             66 سطراً    حالة في الذاكرة + pub/sub + حفظ مؤجّل
        ├── auth.js             213 سطراً    5 شاشات مصادقة
        ├── app.js              341 سطراً    الإقلاع · الشريط الجانبي · الموجّه · اللوحة
        │
        ├── rooms.js            474 سطراً    شبكة الغرف + التفاصيل + المخزون + CSV
        ├── guests.js           330 سطراً    ملفات النزلاء + السجلات
        ├── reservations.js     782 سطراً    محرّك الحجز + الفواتير + التقييمات
        ├── inventory.js        450 سطراً    5 أنواع سندات + الحدود الدنيا + السجل
        ├── coffeeshop.js       408 أسطر     نقطة بيع + قائمة + 3 طرق تسوية
        ├── laundry.js          282 سطراً    معاملات + تسعير + سجل
        ├── maintenance.js      301 سطر      تذاكر + أتمتة حالة الغرفة + فوترة
        ├── hr.js               537 سطراً    دليل + حضور + رواتب + ورديات
        ├── accounting.js       310 أسطر     دفتر أستاذ + تدقيق ليلي + سجل تصحيحات
        └── companies.js        367 سطراً    سجل شركات + عقود + شيكات
```

**ترتيب تحميل السكربتات إلزامي** (`src/index.html:59-78`):
```
mock-main.js → preload.js → i18n → util → store → auth →
rooms → settings → guests → reservations → inventory →
coffeeshop → laundry → maintenance → hr → accounting →
companies → app
```
`app.js` **أخيراً دائماً** لأنه يستدعي `Alaseel.<module>.mount()` لكل الوحدات.

---

## 10. الثغرات والمشاكل الحالية

> هذا القسم موثّق بدقة بمسارات وأرقام أسطر — كل بند تم التحقق منه في الكود.

### 🔴 عاجل — النظام لا يعمل حالياً

#### 10.1 ملف `mock-main.js` مفقود
`src/index.html:59` يستدعي:
```html
<script src="mock-main.js"></script>
```
**هذا الملف غير موجود في المستودع إطلاقاً** → خطأ 404 عند التحميل.

#### 10.2 التطبيق ينهار مباشرة بعد تسجيل الدخول

سلسلة الفشل الكاملة:

```
store.init() → alaseelAPI.loadData()
    ↓
المحاكاة ترجع: { settings:{theme,language}, auth:{isLoggedIn:false} }
    ↓  ← لا يوجد rooms ولا guests ولا reservations!
تسجيل الدخول ينجح (المحاكاة ترجع ok:true لأي مدخلات)
    ↓
onAuthenticated() → initShell() → navigateTo('dashboard')
    ↓
renderDashboard():  const rooms = Alaseel.store.get().rooms;   // undefined
    ↓
Alaseel.rooms.computeKpis(undefined)
    ↓
💥 TypeError: Cannot read properties of undefined (reading 'filter')
```

**الموقع:** `src/js/app.js:224-225` ← `src/js/rooms.js:41`

**السبب الجذري:** المحاكاة لا تولّد الشكل الافتراضي للبيانات. الدالة `defaultData()` التي تولّد 82 غرفة تعيش في `src/main.js:131-178` ولا تُنفَّذ إطلاقاً في نسخة الويب.

#### 10.3 دالة الحفظ في المحاكاة معطوبة

**التوقيع لا يتطابق:**
```javascript
// store.js:47 يستدعي:
window.alaseelAPI.saveData(state)              // وسيط واحد

// preload.js:7 يعرّف:
saveData: (key, data) => { ... }               // وسيطان
```
النتيجة: `key` = كائن الحالة كاملاً، و `data` = `undefined`.
`localStorage.setItem(key, JSON.stringify(undefined))` → يُخزَّن تحت المفتاح `"[object Object]"` بقيمة `"undefined"`.

**⚠️ كل التعديلات تُفقد عند إعادة تحميل الصفحة.**

#### 10.4 قسم الأمان في الإعدادات معطوب

مشكلتان متراكبتان في `src/js/settings.js`:

**(أ) قراءة حقل محذوف** — السطر 74:
```javascript
value="' + esc(d.auth.username || '') + '"
```
بعد ترحيل v5→v6 أصبح `data.auth = { recovery_hash }` فقط، و `username` انتقل إلى `users[]` (`src/main.js:227-238`). النتيجة: الحقل يظهر فارغاً دائماً، وقد يرمي استثناءً إن كان `auth` غير معرّف.

**(ب) استدعاء ناقص الوسائط** — السطر 140:
```javascript
await window.alaseelAPI.authChangePassword({ currentPassword, newUsername, newPassword });
```
لكن المعالج في `src/main.js:435` يتوقّع `userId` ويستخدمه للبحث:
```javascript
const user = data.users.find((u) => u.user_id === userId);
if (!user) return { ok: false, error: 'NOT_INITIALIZED' };
```
**النتيجة: تغيير كلمة المرور يفشل دائماً** — حتى في نسخة Electron الأصلية.

#### 10.5 مسارات Electron معطوبة بعد نقل الملفات

`commit 2b69a63` نقل `main.js` و `preload.js` من الجذر إلى `src/` **دون تحديث أي مسار**:

| الموقع | الكود | ينتج | المفروض |
|---|---|---|---|
| `package.json:5` | `"main": "main.js"` | ملف غير موجود بالجذر | `"src/main.js"` |
| `src/main.js:309` | `loadFile(join(__dirname,'src','index.html'))` | `src/src/index.html` ❌ | `join(__dirname,'index.html')` |
| `src/main.js:295` | `icon: join(__dirname,'assets','icons','icon.png')` | `src/assets/...` ❌ | `join(__dirname,'..','assets',...)` |
| `package.json:28-33` | `files: ["main.js","preload.js","src/**/*",...]` | تكرار + مسارات خاطئة | تحديث القائمة |

**النتيجة: `npm start` لا يعمل.**

---

### 🟠 وظائف ناقصة

#### 10.6 ثماني دوال API مفقودة في المحاكاة

| الدالة | من يستدعيها | ما يتعطّل |
|---|---|---|
| `authVerifyRecovery` | `auth.js:166` | استرداد الحساب المنسي |
| `authReset` | `auth.js:205` | إعادة تعيين كلمة المرور |
| `authChangePassword` | `settings.js:140` | تغيير بيانات الدخول |
| `authCreateUser` | — (لا واجهة) | إنشاء مستخدمين |
| `authDeleteUser` | — (لا واجهة) | حذف مستخدمين |
| `exportRoomsCsv` | `rooms.js:271` | تصدير بيانات الغرف |
| `exportLedgerCsv` | `accounting.js:165` | تصدير دفتر الأستاذ |
| `revealInFolder` | `rooms.js:278`, `accounting.js:169` | فتح مجلد الملف المصدَّر |

#### 10.7 تبويب "سند الاستلام" غير منفَّذ

النصوص كاملة في `i18n.inventory` (`intake_title`, `intake_hint`, `intake_submit`, `intake_success`)، وزر التبويب يُبنى تلقائياً من `Object.keys(T().tabs)` (`src/js/inventory.js:36`)، لكن `renderActiveTab()` (السطور 48-56) **لا يحتوي فرعاً لـ `intake`** → النقر عليه يعطي لوحة فارغة.

**الأثر الوظيفي:** لا توجد طريقة رسمية لإدخال توريد جديد من مورّد. الحل الالتفافي الوحيد: تعديل مخزون غرفة المخزن يدوياً من وحدة الغرف.

#### 10.8 تبويب "سجل الطلبات" في المقهى غير معروض

`i18n.coffeeShop.tabs.history` معرّف، والبيانات تُجمَّع في `orderHistory` عند كل تسوية، لكن `render()` (`src/js/coffeeshop.js:34-37`) يبني تبويبَين فقط يدوياً بدل التوليد من `tabs`.

#### 10.9 تنبيهات الشيكات المتأخرة لا تظهر في لوحة المعلومات

- النصوص موجودة: `i18n.dashboard.corporate_alerts_title`, `no_corporate_alerts`, `overdue_by_prefix/suffix`
- الدالة جاهزة: `Corporate.overdueChecks()` (`src/js/companies.js:348-359`)
- **لكن `renderDashboard()` لا يستدعيها**

#### 10.10 أزرار الشيكات مفقودة

`Corporate.markCheckPaid()` و `Corporate.addPenalty()` معرّفتان بالكامل، والنصوص موجودة (`mark_check_paid`, `add_penalty`, `penalty_added`, `penalty_amount_required`)، لكن `checkRow()` (`src/js/companies.js:208-219`) **لا يرسم أي زر** → الشيك يبقى `pending` للأبد.

#### 10.11 لا واجهة لإدارة المستخدمين

معالجات `auth:createUser` و `auth:deleteUser` مكتوبة في `main.js` مع فحص `USERNAME_TAKEN` و `LAST_ADMIN`، لكن **لا يوجد أي كود واجهة يستدعيها**. لا يمكن إنشاء حسابات موظفين إطلاقاً.

#### 10.12 الصلاحيات لا تُفرض

`employee.permissions[]` تُحفظ من واجهة الموظفين، و `users[].role` يُخزَّن — لكن **لا شيء في الواجهة يفحصهما**. أي مستخدم مسجّل الدخول يرى ويعدّل كل الوحدات.

#### 10.13 بحث معطّل في 5 وحدات

`src/js/app.js:114-128` يوجّه البحث لـ `inventory`, `coffee_shop`, `laundry`, `maintenance`, `accounting` — لكن كل هذه الوحدات تعرّف:
```javascript
onSearch() { /* no search surface in this module */ }
```
الكتابة في مربع البحث لا تفعل شيئاً.

---

### 🟡 عدم اتساق منطقي

#### 10.14 التدقيق الليلي: النص يخالف السلوك

نص الواجهة (`i18n.accounting.night_audit_hint`) يقول:
> «... **وقيود الفاقد من المخزون** ...»

بينما الكود يستبعدها عمداً بتعليل مكتوب (`src/js/accounting.js:9-12`). **النص هو ما يجب تصحيحه**، لأن القرار البرمجي صحيح.

#### 10.15 عملات مختلطة في التدقيق الليلي

- إيراد الغرفة: يستخدم `r.currency` الحقيقي ✅
- المقهى / المغسلة / الصيانة: `currency: 'EGP'` مُثبَّتة (`src/js/accounting.js:231`)

معاملة نقدية بعملة أخرى تُسجَّل كجنيه مصري خطأً.

#### 10.16 العربون لا يُخصم

`deposit_amount` يُدخل ويُحفظ، لكن `folioGrandTotal()` (`src/js/util.js:113-116`) لا يطرحه:
```javascript
return (reservation.total_amount || 0) + extras;   // العربون غائب
```

#### 10.17 حالة `no_show` بلا أتمتة

قابلة للاختيار يدوياً من قائمة الحالات، لكن لا توجد أي أتمتة تحوّل حجزاً `confirmed` مضى تاريخ وصوله إلى `no_show`، ولا سياسة رسوم عدم الحضور.

#### 10.18 كود دالة ميت

`renderStub()` (`src/js/app.js:248-257`) يقرأ `item.phase`، لكن كل عناصر `NAV_ITEMS` لديها `built: true` ولا أحد منها يحمل `phase` → الدالة لا تُستدعى أبداً وستعرض `undefined` لو استُدعيت.

#### 10.19 حقول تُجمع ولا تُستهلك

| الحقل | المكان | الحالة |
|---|---|---|
| `company.fixed_discount` | الشركات | يُحفظ، **لا يُطبَّق على أي فاتورة** (معلن في تعليق الملف) |
| `reservation.promo_code` | الحجوزات | يُحفظ، **لا يخصم شيئاً** |
| `hotel.logo_variant` | الإعدادات | يُخزَّن، لا يُقرأ |
| `locationStock.laundry_transit` | المخزون | يُهيَّأ، **لا وحدة تستخدمه** |
| `check.penalty_fees[]` | الشركات | البنية موجودة، لا واجهة تملؤها |

#### 10.20 `performed_by` و `approved_by` دائماً `null`

كلاهما يقرأ `d.auth.username` (`src/js/util.js:161`, `src/js/hr.js:403`) — الحقل الذي أُزيل في ترحيل v6. **لا توجد جلسة مستخدم حالية محفوظة في الحالة أصلاً** — `onAuthenticated()` لا يخزّن الكائن العائد من تسجيل الدخول.

---

## 11. ما لم يُنفّذ بعد — خارطة الطريق

### المرحلة أ — الإصلاحات الحرجة (شرط لأي تقدّم)

| # | البند | الأولوية |
|---|---|---|
| 1 | إنشاء `mock-main.js` أو إزالة استدعائه | 🔴 |
| 2 | جعل المحاكاة تولّد `defaultData()` كاملة (82 غرفة + كل المصفوفات) | 🔴 |
| 3 | إصلاح توقيع `saveData` ليطابق `store.js` | 🔴 |
| 4 | تصحيح مسارات Electron (`package.json` + `main.js`) | 🔴 |
| 5 | إصلاح `authChangePassword` (تمرير `userId` + قراءة الاسم من `users[]`) | 🔴 |
| 6 | استكمال الدوال الثماني المفقودة في المحاكاة | 🟠 |
| 7 | حفظ المستخدم الحالي في الحالة بعد تسجيل الدخول | 🟠 |

### المرحلة ب — إكمال الوحدات القائمة

| # | البند |
|---|---|
| 1 | **تنفيذ سند الاستلام** — الطريق الوحيد لإدخال توريد جديد |
| 2 | **عرض تبويب سجل طلبات المقهى** |
| 3 | **أزرار الشيكات** — "تحديد كمدفوع" + "إضافة ضريبة تأخير" |
| 4 | **تنبيهات الشيكات والعقود المنتهية في لوحة المعلومات** |
| 5 | **واجهة إدارة المستخدمين** (إنشاء / حذف / تعديل دور) |
| 6 | **فرض الصلاحيات** — إخفاء عناصر الشريط الجانبي حسب الدور |
| 7 | **بحث فعلي** في الوحدات الخمس التي تربطه بلا تنفيذ |
| 8 | خصم العربون من إجمالي الفاتورة |
| 9 | تفعيل `fixed_discount` و `promo_code` |
| 10 | دعم عملة حقيقية في كل قيود التدقيق الليلي |
| 11 | تصحيح نص التدقيق الليلي بخصوص الفاقد |
| 12 | ترحيل الترحيل لفاتورة شركة إلى المغسلة أيضاً |

### المرحلة ج — الوظائف الفندقية المفقودة

| # | البند | الوصف |
|---|---|---|
| 1 | **الطباعة والفواتير** | فاتورة PDF للنزيل، إيصال نقطة بيع، تقرير التدقيق الليلي |
| 2 | **النسخ الاحتياطي والاستعادة** | تصدير/استيراد ملف البيانات من الواجهة |
| 3 | **مخطط التوافر (Availability Chart)** | عرض تقويمي للغرف مقابل التواريخ |
| 4 | **إدارة التدبير المنزلي** | تخصيص غرف للعاملات، متابعة إنجاز التنظيف |
| 5 | **تقارير تحليلية** | ADR · RevPAR · نسب الإشغال التاريخية · إيراد لكل قسم |
| 6 | **سجل تدقيق شامل** | من غيّر ماذا ومتى عبر كل الوحدات (موجود للفواتير فقط) |
| 7 | **أتمتة `no_show`** | تحويل تلقائي + سياسة رسوم |
| 8 | **إدارة المجموعات** | حجز عدة غرف بفاتورة واحدة |
| 9 | **كتل غرف الشركات** | حجز مسبق لكتلة غرف بعقد |
| 10 | **تكلفة أصناف المخزون** | سعر تكلفة لكل صنف → يفتح ترحيل خسائر الفاقد محاسبياً |
| 11 | **إشعارات النفاد التلقائية** | تنبيه استباقي بدل الفحص اليدوي |
| 12 | **دورة العهدة (Custody)** | ربط أصناف بموظف مسؤول |

### المرحلة د — البنية التحتية والجودة

| # | البند |
|---|---|
| 1 | **حزمة اختبارات** — لا يوجد أي اختبار حالياً |
| 2 | **معالجة أخطاء شاملة** — لا يوجد `try/catch` حول عمليات الواجهة |
| 3 | **حالات تحميل** — لا مؤشرات أثناء العمليات غير المتزامنة |
| 4 | **تحسين الأداء** — إعادة رسم كاملة عند كل تعديل (مقبولة لـ 82 غرفة، تنهار مع آلاف الحجوزات) |
| 5 | **إمكانية الوصول (a11y)** — لا `aria-label`، لا تنقل بلوحة المفاتيح، لا إدارة تركيز |
| 6 | **بناء وتوزيع** — إعداد `electron-builder` غير مُختبَر بعد نقل الملفات |
| 7 | **التحديث التلقائي** — غير موجود |
| 8 | **تشفير ملف البيانات** — الملف نص عادي؛ بيانات جوازات النزلاء غير مشفَّرة على القرص |
| 9 | **دعم الشبكة المحلية** — النظام أحادي الجهاز؛ فندق حقيقي يحتاج عدة محطات |
| 10 | **تعدّد اللغات** — البنية جاهزة (`i18n.js`) لكن العربية فقط مطبَّقة |

### المرحلة هـ — التوسّع بعيد المدى

- **تعدّد المنشآت** (Multi-Property) من نسخة واحدة
- **ربط قنوات الحجز** (Booking.com / Expedia / Channel Manager)
- **بوابة دفع إلكترونية**
- **تطبيق جوال للموظفين** (خدمة الغرف والتدبير المنزلي)
- **بوابة النزيل الذاتية** (تسجيل وصول ذاتي، طلبات خدمة)
- **تكامل الفوترة الإلكترونية الحكومية** (ETA المصرية)
- **الذكاء الاصطناعي للتسعير الديناميكي**

---

## 12. التشغيل والبناء والنشر

### 12.1 المتطلبات
- Node.js 18+
- npm

### 12.2 التثبيت
```bash
npm install
```

### 12.3 التشغيل — نسخة سطح المكتب

> ⚠️ **لا يعمل حالياً** — راجع [10.5](#105-مسارات-electron-معطوبة-بعد-نقل-الملفات).

```bash
npm start        # electron .
```

**الإصلاح المطلوب:**
```jsonc
// package.json
"main": "src/main.js",
"build": {
  "files": ["src/**/*", "assets/**/*", "package.json"]
}
```
```javascript
// src/main.js:309
mainWindow.loadFile(path.join(__dirname, 'index.html'));
// src/main.js:295
icon: path.join(__dirname, '..', 'assets', 'icons', 'icon.png'),
```

### 12.4 وضع التصحيح
```bash
ALASEEL_DEBUG=1 npm start     # يفتح DevTools في نافذة منفصلة
```

### 12.5 بناء المُثبِّت
```bash
npm run dist     # electron-builder → dist/
```
| الإعداد | القيمة |
|---|---|
| `appId` | `com.alaseel.pms` |
| `productName` | `Alaseel PMS` |
| الهدف | Windows NSIS |
| `oneClick` | `false` (مثبِّت تفاعلي) |
| `allowToChangeInstallationDirectory` | `true` |

### 12.6 التشغيل — نسخة الويب
أي خادم ملفات ثابتة يخدم مجلد `src/`:
```bash
npx serve src
```
> ⚠️ ينهار بعد تسجيل الدخول — راجع [10.2](#102-التطبيق-ينهار-مباشرة-بعد-تسجيل-الدخول).

### 12.7 مواقع البيانات

| البيئة | المسار |
|---|---|
| **Windows** | `%APPDATA%\Alaseel PMS\alaseel-data.json` |
| **macOS** | `~/Library/Application Support/Alaseel PMS/alaseel-data.json` |
| **Linux** | `~/.config/Alaseel PMS/alaseel-data.json` |
| **التصديرات** | `~/Documents/Alaseel-PMS-Exports/` |
| **الويب** | `localStorage['alaseel_pms_data']` |

---

## 13. المصطلحات

| المصطلح | التعريف |
|---|---|
| **PMS** | Property Management System — نظام إدارة المنشأة الفندقية |
| **Folio** | الفاتورة المفتوحة للنزيل أثناء إقامته، تتراكم عليها كل رسوم الخدمات |
| **Night Audit** | التدقيق الليلي — إجراء يومي يقفل اليوم التشغيلي ويرحّل إيراداته |
| **Operational Date** | التاريخ التشغيلي — يوم العمل الفندقي؛ يتقدّم بالتدقيق الليلي لا بمنتصف الليل |
| **Voucher** | سند — مستند رسمي يوثّق حركة مخزون |
| **Room Status** | حالة الغرفة التشغيلية (جاهزة/مشغولة/تحتاج تنظيف/صيانة/خارج الخدمة) |
| **Blacklist** | قائمة النزلاء الممنوعين من الإقامة |
| **VIP Level** | تصنيف النزيل (عادي/فضي/ذهبي/بلاتيني) |
| **Sentiment** | مؤشر رضا محسوب من تقييمات الغرفة (يتطلب تقييمين على الأقل قبل الوسم) |
| **White-Label** | قابلية إعادة العلامة التجارية لبيع النظام لفنادق أخرى |
| **Contract Term** | مدة عقد الشركة (شهر/ربع/موسم/نصف سنة/سنة) |
| **Maturity Date** | تاريخ استحقاق الشيك البنكي |
| **Draft Pattern** | نمط المسوّدة — الكيان الجديد في الذاكرة حتى الحفظ الصريح |
| **Idempotent** | عملية إعادة تنفيذها لا تغيّر النتيجة (كترحيل المخطط) |

---

## ملخص تنفيذي

| البند | الحالة |
|---|---|
| **الوحدات المكتملة معمارياً** | 12 من 12 |
| **إجمالي أسطر الكود** | ~7,356 |
| **إصدار مخطط البيانات** | 11 |
| **الاعتماديات الخارجية** | صفر (عدا Electron نفسه) |
| **الحالة التشغيلية للنسخة الحالية** | ❌ **لا تعمل** — تنهار بعد تسجيل الدخول |
| **العائق الأساسي** | تحويل غير مكتمل من Electron إلى الويب |
| **الجهد المقدَّر للإصلاحات الحرجة** | صغير — 7 بنود محدَّدة في المرحلة أ |

**التقييم:** المنطق الفندقي في هذا النظام **ناضج ومدروس بعمق** — موجّه الفواتير الموحّد، الحماية من الازدواج المحاسبي، أتمتة حالة الغرف التي تحترم وجود النزيل، ورفض اختراع أرقام مالية غير موجودة في البيانات — كلها قرارات هندسية على مستوى المنتجات التجارية. المشكلة الحالية **ليست في التصميم بل في تحويل تقني غير مكتمل**، وهي قابلة للإصلاح بسبعة تغييرات محدَّدة.

---

*آخر تحديث: 2026-08-15 — تم توليد هذا التوثيق بقراءة كاملة لجميع ملفات المستودع وتاريخ الـ Git.*
